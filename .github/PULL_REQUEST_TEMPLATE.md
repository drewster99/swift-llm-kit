## What and why

<!-- What does this change, and why? Link the issue it closes: "Closes #123". -->

## Checklist

- [ ] `swift test` passes locally with no new warnings
- [ ] New behavior and bug fixes have tests
- [ ] Public API has `///` doc comments
- [ ] Persisted types still decode older files (`decodeIfPresent` for new fields)
- [ ] If I added a `ProviderAPIType` case or a model capability, I updated every place CLAUDE.md lists
